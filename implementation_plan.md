# Build a Portfolio using Vite

You requested to build a portfolio using Vite. The current workspace (`c:\Users\ShanmukhaPhanindra\maze-game`) appears to be a Unity project. Therefore, I propose creating the new portfolio in a dedicated folder. In addition, I noticed you have `My_Resume.pdf` open, and we can extract its contents to populate the portfolio!

## User Review Required

> [!IMPORTANT]
> **Project Location:** I currently plan to create the portfolio at `c:\Users\ShanmukhaPhanindra\portfolio` instead of inside the `maze-game` repository. Is this okay?
> 
> **Resume Integration:** I can use a Python script to parse `c:\Users\ShanmukhaPhanindra\OneDrive\Documents\My_Resume.pdf` to automatically populate your portfolio's "About Me" and "Experience" sections. Would you like me to do this?
>
> **Technology Stack:** I will use Vite to initialize the project, using a Vanilla JavaScript + Vanilla CSS setup for a beautiful, rich design (or React + Vanilla CSS if you prefer components). Let me know if you have a framework preference!

## Proposed Changes

### Setup Portfolio Project
- Create a new Vite project in `C:\Users\ShanmukhaPhanindra\portfolio` (or your preferred directory).
- Install any necessary dependencies (e.g., `npx -y create-vite@latest . --template vanilla`).
- Initialize a beautiful, responsive design with dynamic animations, modern typography, and a cohesive color palette.

### Ingestion of Resume (Optional)
- Read and extract text from `My_Resume.pdf` to extract names, experiences, education, and skills.
- Incorporate this data into the portfolio's static HTML/JS structure.

## Open Questions

- Where exactly would you like the portfolio directory created?
- Would you like the portfolio to be a single-page scrolling site or multi-page?
- Do you have any specific color schemes or aesthetics in mind (e.g., dark mode, glassmorphism, specific vibrant colors)?

## Verification Plan

### Manual Verification
- We will start the Vite dev server (`npm run dev`).
- We can preview the project in the browser to ensure the layout is responsive, animations are smooth, and the data from your resume is correctly populated.
